<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Feed_The_hunger"
        android:usesCleartextTraffic="true">

        <!-- Splash screen -->
        <activity
            android:name=".activity_splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other activities -->
        <activity android:name=".Volenteer.activity_food" android:exported="true" />
        <activity android:name=".Volenteer.activity_volenteer_dashboard" android:exported="true" />
        <activity android:name=".User.activity_user_dashboard" android:exported="true" />
        <activity android:name=".User.User_Login" android:exported="true" />
        <activity android:name=".activity_choose" android:exported="true" />
        <activity android:name=".Volenteer.volenteer_Forgetpassword" android:exported="false" />
        <activity android:name=".User.User_Registration" android:exported="true" />
        <activity android:name=".Volenteer.Volenteer_Login" android:exported="true" />

        <!-- Missing registration activity added -->
        <activity android:name=".Volenteer.Volenteer_Registration" android:exported="true" />

    </application>

</manifest>
